# PRD · 每周知识复习工具（Demo 版）

> 状态：草稿，待评审。本文件是交给 Claude Code 落地的依据之一。

## 1. 这个 demo 要证明什么

一个陌生人把自己和 AI 的一段对话丢进来，能立刻拿到一份**自然、像人话写的知识复习**——它自动挑出「值得回看的知识类提问」、剔掉「让 AI 干活」的，按主题归堆，末尾附一个小测验做主动回忆。

demo 的成败只看一件事：**这份输出好到让人愿意每周都要一次。** 不测采集、不测留存、不测规模。

## 2. 范围

**范围内**
- 输入：**只有粘贴**。用户从任意平台（Claude / ChatGPT / 豆包 / DeepSeek …）复制一段对话，贴进来即可。
- 处理：归一化 → 分类（知识 / 干活 / 其它）→ 按主题归堆 → 生成周报 → 生成 quiz。
- 输出：一个页面，展示周报 + 「它挑掉了哪些干活的」 + 可交互 quiz。
- 无状态：处理完即弃，不落库、不留对话内容。

**范围外（明确推迟，不在 demo 做）**
- 登录 / 账号 / 多用户数据隔离。
- **上传导出文件**——导出是异步的（在设置里点、发邮件、等数分钟到数小时）、且是全量历史，对「当场就想看结果」的 demo 是致命摩擦，也和「这一周」的场景不符。砍掉，只留粘贴。
- 浏览器插件、手机分享（PWA / 快捷指令）等**自动采集**——demo 入口是零安装的粘贴。
- 每周定时 Cron、发邮件、存档页。
- **taste 纠正闭环**（分类的自我校准）——这是产品的差异化壁垒，但要等基础输出被验证喜欢后再叠加。
- 数据库、rubric 版本管理。

## 3. 用户流程（happy path）

1. 打开首页，看到一句话说明 + 一个大输入框（粘贴）。
2. 从任意 AI 里复制一段对话贴进来，点「生成复习」。
3. 等几秒，看到：本周复习正文（自然叙述、按主题分段）→ 一个可折叠的「已过滤 N 条干活/闲聊」→ quiz。
4. 做 quiz：逐题先自己想 → 点开看答案 → 自评记住/没记住。
5. 页面底部一句话：想每周自动收到这个？（留个邮箱 / 关注入口——仅收集意向，demo 不真发）。

## 4. 功能需求

**4.1 输入与归一化**
- 只处理粘贴的纯文本。**不追求精确的问答配对**：代码侧只做最粗的分块（按空行切段），把每段当成一个候选 item，语义识别交给分类那步的 prompt。这样代码几乎没有解析逻辑，也更稳。
- 每段映射为 `NormalizedItem`：`question` 存这段原文，`source='paste'`，无 `answer`/`createdAt`（粘贴没有时间信息，demo 不跨周聚合，无影响）。
- 对超长输入做段数/字数上限保护（见 §6 成本）。

**4.2 分类**
- 用分类 prompt（见交接包）判定每条为 knowledge / task / other，并打主题标签。
- demo 用内置默认判定标准，不加载任何用户纠正（taste 闭环推迟）。

**4.3 周报生成**
- 用写周报 prompt 生成正文：自然连贯的中文，按主题分段，**不要**满屏加粗小标题和项目符号。
- 忠于素材内容，素材没讲到的不硬编。

**4.4 Quiz（主动回忆）**
- 基于本周知识点生成 3–5 题。
- 默认形式：**闪卡式**——展示问题，用户先自答，点击「看答案」揭示参考答案，自评「记住了 / 没记住」。
- 待定（评审决定）：是否改用/增加「简答 + 模型批改反馈」（体验更好，每题多一次 API 调用）或「选择题自动判分」。
- 「没记住」的题可一键收进一个简单的复习清单（仅前端内存，不落库）。

## 5. AI / 模型

- 分类：`claude-haiku-4-5-20251001`（量大、便宜、快）。
- 周报 + quiz 生成：`claude-sonnet-4-6`（要语气和质量）。
- 两段核心 prompt（分类、写周报/quiz）由交接包提供，Claude Code 不要自行重写。

## 6. 非功能需求

**成本 / 防滥用**（公开接口，每次请求都在烧额度，必须有）
- 单 IP / 单会话限流（如每 IP 每天 N 次）。
- 全局每日预算护栏：当天调用超阈值自动降级/暂停并给友好提示。
- 输入上限：单次最多 X 条 / Y 字，超出提示裁剪。

**隐私**（别人会贴进私密对话，是信任的关键）
- 对话内容**只在内存中处理，不落库、不写日志**。
- 首页明确写出这一点。

**性能**
- 端到端出结果目标 < 10 秒；分类与生成可并行/流式，先出周报再出 quiz 也可接受。

## 7. 输入格式契约

所有来源最终归一化成：

```ts
interface NormalizedItem {
  id: string;
  question: string;            // 用户发的那条
  answer?: string;             // AI 的回答（有则复习更实）
  source?: string;             // 'claude' | 'chatgpt' | 'paste' ...
  conversationTitle?: string;
  createdAt?: string;          // ISO
}
```

Demo 只有粘贴一条来源：按空行粗切成段，每段 → 一个 `NormalizedItem`（`question`=段落原文，`source='paste'`）。不做精确问答配对，是否含知识提问交给分类器判。（导出适配器等真正做产品时再说，届时走自动采集，导出基本用不上。）

## 8. 验收标准

- 贴入一段混了知识提问和干活请求的对话，干活的被正确剔掉，知识的进了周报。
- 周报读起来像人写的，不是 AI 模板腔（无满屏加粗/项目符号）。
- quiz 的题确实来自本周内容，答案正确、能自评。
- 全程无需登录；刷新后不残留上一次的对话内容。
- 触发限流/超长/空输入时都有清楚的提示，不白屏、不报错崩。

## 9. Demo 之后的路线（不在本 PRD 实现，仅备忘）

采集层（浏览器插件 + 手机快捷指令）→ 账号与按用户隔离（Supabase Auth + RLS）→ taste 纠正闭环（每用户 corrections / rubric）→ 每周 Cron + 邮件 + 存档页 → 规则蒸馏。
